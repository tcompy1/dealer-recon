# Hiley Demo Notes

Status note: archive candidate. These notes include stale dashboard metric, trend delta, and reviewer workload ideas that should not center v1. Use `docs/product/fp-rec-four-step-workflow.md` for current scope.

## web app demo

can we make run id mm/yy?

things we don't need starting from step 3:
    - match rate
    - match rate change
    - Avg resolution
    - improved since last run

in category trend we can remove the previous and delta columns, keep the "current column"

remove reviewer workload

## FP REC Export review

the review doesn't balance correctly for april
BOA report should not be credits it should be debits
debit/credits out of balance

difference = statement subtotal + subtotal

outstanding pre store should be a debit balance
